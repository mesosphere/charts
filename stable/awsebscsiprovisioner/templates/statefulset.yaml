---
kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: ebs-csi-controller
  namespace: {{ .Release.Namespace }}
spec:
  serviceName: ebs-csi-controller
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: ebs-csi-controller
  template:
    metadata:
    {{- if .Values.statefulSetCSIController.podAnnotations }}
      annotations:
      {{- range $key, $value := .Values.statefulSetCSIController.podAnnotations }}
        {{ $key }}: {{ $value | quote }}
      {{- end }}
    {{- end }}
      labels:
        app: ebs-csi-controller
        app.kubernetes.io/name: {{ include "aws-ebs-csi-driver.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      nodeSelector:
        beta.kubernetes.io/os: linux
      serviceAccount: ebs-csi-controller-sa
      priorityClassName: system-cluster-critical
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      containers:
        - name: ebs-plugin
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          args :
            - controller
            - --endpoint=$(CSI_ENDPOINT)
            {{ include "aws-ebs-csi-driver.extra-volume-tags" . }}
            - --logtostderr
            - --v=5
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key | quote }}
              value: {{ tpl $value $ | quote }}
            {{- end }}
            - name: CSI_ENDPOINT
              value: unix:///var/lib/csi/sockets/pluginproxy/csi.sock
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: aws-secret
                  key: key_id
                  optional: true
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: aws-secret
                  key: access_key
                  optional: true
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
          ports:
            - name: healthz
              containerPort: 9808
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: healthz
            initialDelaySeconds: 10
            timeoutSeconds: 3
            periodSeconds: 10
            failureThreshold: 5
        - name: csi-provisioner
          image: "{{ .Values.provisioner.image.repository }}:{{ include "aws-ebs-csi-driver.csi-provisioner.tag" . }}"
          args:
            - --csi-address=$(ADDRESS)
            - --v=5
            {{- if .Values.provisioner.enableVolumeScheduling }}
            - --feature-gates=Topology=true
            {{- end}}
            {{- if (gt (.Values.replicas | int) 1) }}
            {{- if semverCompare ">=2.0.0" (trimPrefix "v" (include "aws-ebs-csi-driver.csi-provisioner.tag" .)) }}
            - --leader-election=true
            {{- else }}
            - --enable-leader-election
            - --leader-election-type=leases
            {{- end }}
            {{- end }}
          env:
            - name: ADDRESS
              value: /var/lib/csi/sockets/pluginproxy/csi.sock
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
        - name: csi-attacher
          image: "{{ .Values.attacher.image.repository }}:{{ include "aws-ebs-csi-driver.csi-attacher.tag" . }}"
          args:
            - --csi-address=$(ADDRESS)
            - --v=5
            {{- if (gt (.Values.replicas | int) 1) }}
            - --leader-election=true
            {{- end }}
          env:
            - name: ADDRESS
              value: /var/lib/csi/sockets/pluginproxy/csi.sock
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
        {{- if .Values.snapshotter.enabled }}
        - name: csi-snapshotter
          image: "{{ .Values.snapshotter.image.repository }}:{{ include "aws-ebs-csi-driver.snapshotter.tag" . }}"
          args:
            - --csi-address=$(ADDRESS)
            {{- if (gt (.Values.replicas | int) 1) }}
            - --leader-election=true
            {{- end }}
          env:
            - name: ADDRESS
              value: /var/lib/csi/sockets/pluginproxy/csi.sock
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
        {{- end }}
        {{- if .Values.resizer.enabled }}
        - name: csi-resizer
          image: "{{ .Values.resizer.image.repository }}:{{ .Values.resizer.image.tag }}"
          imagePullPolicy: Always
          args:
            - --csi-address=$(ADDRESS)
            - --v=5
            {{- if (gt (.Values.replicas | int) 1) }}
            - --leader-election=true
            {{- end }}
          env:
            - name: ADDRESS
              value: /var/lib/csi/sockets/pluginproxy/csi.sock
          volumeMounts:
            - name: socket-dir
              mountPath: /var/lib/csi/sockets/pluginproxy/
        {{- end }}
        - name: liveness-probe
          image: "{{ .Values.liveness.image.repository }}:{{ .Values.liveness.image.tag }}"
          args:
            - --csi-address=/csi/csi.sock
          volumeMounts:
            - name: socket-dir
              mountPath: /csi
      volumes:
        - name: socket-dir
          emptyDir: {}
