---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: opsportal-landing
  name: opsportal-landing
spec:
  ports:
    - name: opsportal-landing
      port: {{ .Values.landing.port }}
      protocol: TCP
      targetPort: 80
  selector:
    app: opsportal-landing
