apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: autoscalerstress-cron
spec:
  schedule: {{ .Values.autoscalerjob.schedule }}
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: {{ .Values.autoscalerjob.name }}
            image: TBD
            tag: {{ .Values.autoscalerjob.tag }}
            {{- with .Values.autoscalerjob.args }}
            args:
{{ toYaml . | indent 12 }}
              {{- end }}
          restartPolicy: {{.Values.autoscalerjob.restartPolicy}}
