---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: applicationsnapshotrestores.dataservices.nutanix.com
spec:
  group: dataservices.nutanix.com
  names:
    kind: ApplicationSnapshotRestore
    listKind: ApplicationSnapshotRestoreList
    plural: applicationsnapshotrestores
    shortNames:
    - asr
    singular: applicationsnapshotrestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Snapshot name
      jsonPath: .spec.applicationSnapshotName
      name: Snapshot-Name
      type: string
    - description: Is restore done
      jsonPath: .status.completed
      name: Completed
      type: boolean
    - description: Timestamp when the snapshot restoration was started.
      jsonPath: .status.startTime
      name: Recovery-Start-Time
      priority: 1
      type: string
    - description: Timestamp when the snapshot was completely restored
      jsonPath: .status.finishTime
      name: Recovery-Complete-Time
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ApplicationSnapshotRestore is the Schema for the ApplicationSnapshotRestores
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSnapshotRestoreSpec defines the desired state
              of ApplicationSnapshotRestore
            properties:
              applicationSnapshotName:
                description: |-
                  ApplicationSnapshotName specifies the name of the ApplicationSnapshot
                  that must be restored.
                type: string
              applicationSnapshotNamespace:
                description: ApplicationSnapshotNamespace specifies the namespace
                  where the ApplicationSnapshot is present.
                type: string
            required:
            - applicationSnapshotName
            type: object
            x-kubernetes-validations:
            - message: Spec is immutable
              rule: self == oldSelf
          status:
            description: ApplicationSnapshotRestoreStatus defines the observed state
              of ApplicationSnapshotRestore
            properties:
              boundApplication:
                description: BoundApplication references to the restored application
                type: string
              completed:
                description: |-
                  Completed represents whether the restore request has completed
                  successfully or not. A null value means false.
                type: boolean
              conditions:
                description: |-
                  Conditions represent the latest available observations of the ApplicationSnapshotRestore
                  object's state.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: |-
                  Error is the last observed error during snapshot creation, if any.
                  Upon success after retry, this error field will be cleared.
                properties:
                  message:
                    description: |-
                      Message is a string detailing the encountered error during restore
                      if available.
                    type: string
                  reason:
                    description: Reason contains a programmatic identifier indicating
                      the reason for the error.
                    type: string
                  time:
                    description: Time is the timestamp when the error was encountered.
                    format: date-time
                    type: string
                type: object
              finishTime:
                description: FinishTime represents the timestamp when the snapshot
                  was completely restored.
                type: string
              startTime:
                description: StartTime represents the timestamp when the snapshot
                  restoration was started.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
