---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: storageclusters.dataservices.nutanix.com
spec:
  group: dataservices.nutanix.com
  names:
    kind: StorageCluster
    listKind: StorageClusterList
    plural: storageclusters
    singular: storagecluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: If the storage cluster is available for use
      jsonPath: .status.available
      name: Available
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageCluster provides storage capacity.
          This is the entitiy to which storage policies are applied. The effects of a
          storage policy is recorded in status.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              StorageClusterSpec defines how a storage cluster can be identified in underlying
              infrastructure.
            properties:
              managementServerUuid:
                description: |-
                  managementServerUuid to uniquely identify the PC.
                  Can be obtained by running "ncli cluster info" on the PC,
                  and using the value in "Cluster Uuid" field of the output.
                type: string
              storageServerUuid:
                description: |-
                  storageServerUuid to uniquely identify the PE cluster.
                  Can be obtained by running "ncli multicluster get-cluster-state" on the PC,
                  and using the value in "Cluster Id" field of the output.
                type: string
            type: object
            x-kubernetes-validations:
            - message: Spec is required and immutable for storageclusters.dataservices.nutanix.com
              rule: self == oldSelf
          status:
            description: StorageClusterStatus defines the observed state of StorageCluster
            properties:
              available:
                default: false
                description: available indicates if the StorageCluster is in a healthy
                  state and ready to use.
                type: boolean
              message:
                description: A human-readable message indicating details about why
                  the cluster is in this state.
                type: string
              reason:
                description: |-
                  reason is a brief CamelCase string that describes any failure and is meant
                  for machine parsing and tidy display in the CLI.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
