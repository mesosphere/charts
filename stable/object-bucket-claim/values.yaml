dkp:
  # name of apps that consumes the S3 bucket to create
  # needs to be the helmrelease name of the consuming app
  velero:
    enabled: true
    bucketName: dkp-velero
    # generateBucketName: ceph-bkt
    storageClassName: s3-provider-sc
    # needed only for rook-ceph-cluster k-app
    enableOBCHealthCheck: true

    labels: {}
    additionalConfig:
      # maxObjects: "1000"
      # in string format like "2G", minimum is "4K"
      maxSize: "10G"

  grafana-loki:
    enabled: true
    bucketName: dkp-loki
    # generateBucketName: ceph-bkt
    storageClassName: s3-provider-sc
    # needed only for rook-ceph-cluster k-app
    enableOBCHealthCheck: true

    labels: {}
    additionalConfig:
      # maxObjects: "1000"
      # in string format like "2G", minimum is "4K"
      maxSize: "40G"

  # your-app:
  #   enabled: true
  #   bucketName: your-app
  #   # generateBucketName: ceph-bkt
  #   storageClassName: s3-provider-sc

  #   labels: {}
  #   additionalConfig:
  #     # maxObjects: "1000"
  #     # in string format like "2G", minimum is "4K"
  #     maxSize: "2G"
