---
# enable the plugin init container with plugins retrieved from an URL
plugins:
  enabled: true
  reset: false
  # Use <plugin_name,version,url> to add/upgrade plugin
  values:
    - analyze-api-ui-plugin,7.6.0,https://github.com/johtani/analyze-api-ui-plugin/releases/download/7.6.0/analyze_api_ui-7.6.2.zip
    # - other_plugin
