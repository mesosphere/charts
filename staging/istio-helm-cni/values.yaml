# D2iQ Istio CNI Chart Values

global:
  # Used by CNI templates as fallback when cni.hub/tag not set
  hub: docker.io/istio
  tag: 1.29.2
  imagePullPolicy: IfNotPresent
  istioNamespace: istio-system

# CNI component configuration
cni:
  enabled: true
  priorityClassName: "dkp-critical-priority"
